{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Simulation + GtkWave",
            "type": "shell",
            "command": "${env:HOME}/.platformio/penv/bin/platformio run --target=sim",
            "group": {
                "kind": "test",
                "isDefault": true
            }
        }
    ]
}